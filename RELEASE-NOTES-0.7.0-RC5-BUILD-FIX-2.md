# SG-AWG-Panel 0.7.0-RC5 Build Fix 2

- исправлен standalone Cascade между двумя самостоятельными панелями: созданная ссылка сохраняется в базе панели и остаётся видна после обновления страницы до окончания срока действия;
- повторное создание безопасно заменяет предыдущий служебный доступ и сразу показывает новую ссылку;
- удаление служебного доступа очищает сохранённую ссылку;
- импорт Outbound принимает стандартную строку клиентского профиля `AllowedIPs = 0.0.0.0/0, ::/0`;
- служебный `::/0` отбрасывается при нормализации IPv4-only Outbound, остальные IPv6-маршруты по-прежнему блокируются;
- Help и README обновлены;
- добавлены regression tests на сохранение Cascade-ссылки, обновление страницы и IPv4 Outbound со стандартным full-tunnel профилем.
