# SG-AWG-Panel v0.1.0 Alpha 2

Исправления чистой установки:

- хеш пароля больше не подключается как shell-код;
- символы `$` в хеше не ломают установщик;
- Ubuntu 22.04/24.04 проверяется до установки файлов панели;
- AmneziaWG устанавливается и проверяется до web-панели;
- сообщение `Ready` появляется только после успешного запуска web-службы.

Поддерживаемая тестовая система: Ubuntu Server 22.04/24.04.
